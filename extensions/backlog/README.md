# @openclaw/backlog

Backlog.md extension package for OpenClaw.

This package provides plugin registration metadata and a minimal entrypoint so
the extension can be discovered and toggled through the existing plugin system.
